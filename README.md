# ember-data

Unified game-event policy, daily export validation, and operational reports. The backend owns the source tables and exports append-only events; this repository never becomes a gameplay or asset authority.

Reports cover next-day retention, the login/deck/match funnel, PVP result and duration, disconnects, crashes, FPS, and peak memory. Event properties use a controlled allowlist so arbitrary personal data cannot leak into analytics.
